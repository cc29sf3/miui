<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cnki6187.miui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SucActivity" />
        <activity
            android:name=".FailActivity"
            android:label="@string/title_activity_fail"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name="com.example.cnki6187.contentProvider.MyContentProvider"
            android:authorities="com.example.cnki6187.contentProvider.MyContentProvider"
            android:exported="false" />

        <activity
            android:name=".Recycle"
            android:label="@string/title_activity_recycle"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="com.example.cnki6187.francis29" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".FragmentActivity" />
        <activity android:name=".ViewPagerIndicateActivity"></activity>
    </application>

</manifest>